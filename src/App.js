import React, { Component } from 'react';
import DisplayToDoData from './components/AddToDoData/DisplayTODoData';

class App extends Component {
  render() {
    return (
          <div>
          <DisplayToDoData />
        </div> 
    );
  }
}
export default App;

// i Have Done UI for Grouping Drop down with check box in which i tried to search by grouping according
// to priority  but its incompleted am not able to complete grouping 
// sorting is done only according to priority













